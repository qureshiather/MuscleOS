/** One cloud account per email, regardless of Apple / Google / password. */
export const ACCOUNT_PER_EMAIL_COPY =
  'Cloud data is stored per email. Apple, Google, or a password with the same address is the same account.';
